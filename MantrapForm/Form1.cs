namespace MantrapForm
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }

        private void button1_Click(object sender, EventArgs e)
        {

        }

        private void buttonInitCheckPoints_Click(object sender, EventArgs e)
        {
            //call a function to initialize the checkpoints
            // initAllCheckPoints
        }
        private void initAllCheckPoints()
        {
            int totalCount = 2;
            // fod a for loop init all the check points (indicated in the listbox)
            for (int i =0; i<totalCount; i++)
            {
                // CheckPointClient checkpointClient=new checkpointClient()
            }
        }
    }
}